#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int n;

    cin >> n;

    vector<int> p;
    for (int i = 0; i < n; i++)
    {
        int p_;
        cin >> p_;
        p.push_back(p_);
    }

    int k;
    cin >> k;

    

    return 0;
}